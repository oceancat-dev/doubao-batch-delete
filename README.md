# 豆包聊天记录批量删除

豆包网页版目前没有提供聊天记录批量删除功能。本项目是一个本地 Chrome / Edge 扩展，可先批量选择会话，再通过豆包网页现有的“更多 → 删除 → 确认”流程逐条执行。

## 功能

- 扫描当前已经加载的聊天记录
- 自动滚动侧边栏，继续加载历史记录
- 单选、全选和清空选择
- 删除前列出待删除记录，并要求输入 `确认删除`
- 按顺序逐条删除，避免同时操作多个菜单
- 支持中途停止；失败记录会保留并在控制台输出原因
- 不读取 Cookie，不上传聊天内容，也不调用豆包未公开接口

## 安装

1. 从 [Releases](https://github.com/youyang13811808-code/doubao-batch-delete/releases) 下载最新的 `doubao-batch-delete-v*.zip` 并解压。
2. 打开 Chrome 的 `chrome://extensions`，或 Edge 的 `edge://extensions`。
3. 开启右上角“开发者模式”。
4. 点击“加载已解压的扩展程序”，选择刚解压的目录（目录内应直接包含 `manifest.json`）。
5. 登录 [豆包网页版](https://www.doubao.com/chat/) 并刷新页面。

> Chrome / Edge 会限制未经扩展商店签名的扩展直接安装，因此 ZIP 发布包需要先解压，再通过开发者模式加载。

## 使用

1. 右上角会出现“批量管理”面板。
2. 点“加载全部”，等待历史记录滚动加载完成。
3. 勾选需要删除的会话，或点击“全选”。
4. 点“删除所选”，核对记录并按提示输入 `确认删除`。
5. 扩展逐条删除。可随时点“停止任务”；已经删除的记录无法恢复。

## 项目文件

- `manifest.json`：Chrome Manifest V3 扩展配置
- `content.js`：会话识别、选择和删除流程
- `styles.css`：批量管理面板样式

## 注意

- 仅支持 `doubao.com` 网页版；豆包桌面客户端不支持安装浏览器扩展。
- 建议先选 1 条不重要的会话试运行。
- 删除期间不要切换页面、折叠侧栏或手动操作会话菜单。
- 豆包页面改版后，可能需要更新元素识别规则。失败项会保留选择，并在浏览器控制台输出原因。
- 本工具不包含“删除记忆”功能，记忆与聊天记录是两类数据。
- 本项目与豆包及字节跳动没有关联，是非官方辅助工具。

## 更新记录

### v1.0.1

- 修复扩展复选框被误识别为会话“更多”按钮的问题
- 第一条删除失败时立即停止，并在管理面板显示失败阶段
- 增强删除菜单和确认按钮识别，失败后自动关闭残留弹窗

### v1.0.2

- 兼容豆包使用 `div` 或 SVG 图标实现的会话菜单按钮
- 修复具体失败原因被最终汇总覆盖的问题

### v1.0.3

- 排除“技能·连接器·伙伴”等豆包系统导航，避免被误选为聊天记录
- 重新扫描时自动清理旧版本错误插入的复选框

### v1.0.4

- 兼容会话侧栏中无名称、无语义属性的 SVG“更多”控件
- 增强合成悬停事件，适配豆包动态显示菜单按钮的实现

### v1.0.5

- 支持“更多”菜单作为会话行兄弟节点渲染的页面结构
- 通过侧栏几何位置识别与目标会话同行的右侧菜单控件

### v1.0.6

- 删除时强制显示仅在真实鼠标悬停状态下出现的会话操作控件
- 兼容合成鼠标事件无法触发豆包悬停状态的情况

### v1.0.7

- 根据豆包实际 DOM 精确定位 `dropdown-menu-trigger` 菜单按钮
- 临时解除菜单父容器的 `display:none`，修复按钮始终不可见的问题

### v1.0.8

- 等待 Radix 菜单和确认弹窗异步挂载，不再依赖固定延时
- 兼容 `data-slot` 菜单项以及更慢的页面动画

### v1.0.9

- 在“删除”菜单项点击前持续保持菜单触发器可见
- 修复 Radix 因触发器提前隐藏而自动取消菜单的问题

### v1.0.10

- 使用 Radix 菜单实际监听的 `pointerdown` 事件打开会话菜单
- 检查 `aria-expanded` 和 `data-state`，区分按钮识别失败与菜单打开失败

### v1.0.11

- 兼容 Radix `role="alertdialog"` 删除确认弹窗
- 精确识别 `alert-dialog-action`，并支持“删除对话”等按钮文案

### v1.0.12

- 移除会误匹配 Tailwind 类名的 `[class*="dialog"]` 宽泛选择器
- 仅使用真实 `role` 和 `data-slot` 节点确定确认弹窗作用域

### v1.0.13

- 排除没有删除菜单的“主对话”特殊入口
- 单条失败自动跳过并继续，连续失败 3 条才停止任务
- 支持切换到其他标签页后继续运行；豆包标签页必须保持打开

### v1.1.0

- 将菜单、弹窗和删除完成的固定等待改为 DOM 状态监听
- 页面响应后立即处理下一条，减少每条约 1.8 秒的无效等待
- MutationObserver 在后台标签页中也能及时响应页面变化

### v1.1.1

- 区分顶部“新对话”系统入口与历史记录中名为“新对话”的真实会话
- 只要会话行包含删除菜单触发器，即使标题命中系统文案也允许选择
